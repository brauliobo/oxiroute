[package]
name = "oxiroute-loadgen"
version = "0.1.0"
edition = "2024"
rust-version = "1.97"
publish = false

[dependencies]
tokio = { version = "1.53.1", features = ["io-util", "macros", "net", "rt", "time"] }

[workspace]

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
pedantic = "warn"
