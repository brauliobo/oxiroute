server {
    listen 18080;
    server_name whitebeast.lan openwrt.brauliobo.org;

    root /home/braulio/Projects/openwrt/repo;
    autoindex on;
    autoindex_exact_size off;
    autoindex_localtime on;

    location / {
        try_files $uri $uri/ =404;
    }
}

